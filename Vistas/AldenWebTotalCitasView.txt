CREATE VIEW [dbo].[AldenWebTotalCitas]
AS
select ID,Mov,movid,Importe, FechaRequerida from venta where mov ='Cita Servicio'
and FechaRequerida >= convert(char(6), getdate(), 112) + '01'
and Estatus IN ('CONCLUIDO','CANCELADO') and AnexoID is null

GO